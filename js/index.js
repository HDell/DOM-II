// Your code goes here

/*
let element = document.getORqueryByWhatever("element");
element.addEventListener("event", () => {
    console.log("random");
});
element.addEventListener("event", (e) => {
    console.log(e.something);
});
mouseover
keydown
wheel
drag / drop
load
focus
resize
scroll
select
dblclick
*/

/*
Nest two similar events somewhere in the site and prevent the event propagation properly
Stop the navigation items from refreshing the page by using preventDefault()
*/

const navItems = document.querySelectorAll("nav a");
const headerImg = document.querySelector("header img");
const headerParagrah = document.querySelector("header p"); //
const imgContent = document.querySelectorAll(".img-content"); //flip on Y-axis
const destinationHeader = document.querySelector(".content-destination h2");
const buttons = document.querySelectorAll(".content-pick .btn"); //stop Prop here
const ...;
const ...;
const ...;
const ...;
